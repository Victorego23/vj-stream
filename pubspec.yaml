name: vj_stream
description: "VJ STREAM - Plataforma de Streaming de Alta Calidad con Real-Debrid y TMDB"
publish_to: 'none'
version: 2.2.6+5

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2
  cupertino_icons: ^1.0.8
  video_player: ^2.8.2
  shared_preferences: ^2.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
